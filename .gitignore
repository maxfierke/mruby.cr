/docs/
/lib/
/bin/
/.shards/
*.dwarf

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# Built assets
/build/
mruby-out.tar

# Ignore generated bindings for now
src/lib_mruby.cr
